version: "3.9"
services:
  app:
    build:
      dockerfile: Dockerfile
      context: .
    env_file:
      - ".env"
    ports:
      - "8080:5001"
    depends_on:
      - pg_blog
  pg_blog:
    image: postgres:12
    env_file:
    - ".env_test"
    ports:
      - "5433:5432"

